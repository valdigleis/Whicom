/* Declarando variáveis */
x : 10;
y : 5;
z : 2;

/* Realizando operações básica */
soma : x + y;
mult : y * z;
D : 4 -5 

/* Realizando um teste condicional */
if (soma > mult) then {
    alvo : 1
} else {
    alvo : 0
}

if (soma < mult) then {
    alvo : 1
} else {
    alvo : 0
}

if (soma = mult) then {
        alvo:1 }{ else skip; }